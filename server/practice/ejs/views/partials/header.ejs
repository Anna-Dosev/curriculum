<header>
    <div class="y-wrap">
    <% if (isAuthenticated) {%>
        <!-- <p> You are logged in</p> -->
        <button id='auth' data-type="logout">Log Out</button>
    <% } else {%>
        <button id='auth' data-type="login">Log In</button>
    <% } %>
    <ul class="navs">
        <% navs.forEach(nav => { %>
            <li><a href="<%= nav.toLowerCase() %>"><%= nav %></a></li>
        <%}) %>
    </ul>
    </div>
</header>