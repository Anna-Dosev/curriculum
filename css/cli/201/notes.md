# CLI 201

## Create aliases to speed up workflow in command line

Steps to configure an alias in home directory
- touch . alias (already done)
- inside of **.alias** add aliases with this format `alias gb='git branch'`
- import **.alias** intot **.zshrc** or **.bash-profile** witht `.~/. alias`
    this should be at the top of your file